'use strict';

/**
 * ranking-user-voting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ranking-user-voting.ranking-user-voting');
