'use strict';

/**
 * ranking-user-voting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ranking-user-voting.ranking-user-voting');
