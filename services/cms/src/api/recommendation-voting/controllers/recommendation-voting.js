'use strict';

/**
 * recommendation-voting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::recommendation-voting.recommendation-voting');
