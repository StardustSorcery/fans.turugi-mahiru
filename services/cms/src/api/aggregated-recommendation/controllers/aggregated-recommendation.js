'use strict';

/**
 * aggregated-recommendation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::aggregated-recommendation.aggregated-recommendation');
